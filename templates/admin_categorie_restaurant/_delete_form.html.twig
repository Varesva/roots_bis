<form method="post" action="{{ path('app_admin_categorie_restaurant_delete', {'id': categorie_restaurant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_restaurant.id) }}">
<button class="btn btn-danger m-2">Supprimer</button>

</form>
