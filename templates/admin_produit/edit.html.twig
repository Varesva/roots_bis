{% extends 'base.html.twig' %}

{% block title %}Modifier Produit
{% endblock %}


{% block body %}
	<h1>Modifier Produit</h1>
	<section class="container">

		{{ include('admin_produit/_form.html.twig', {'button_label': 'Modifier'}) }}
		<hr>

		<div class="d-flex justify-content-around flex-wrap">

			<a href="{{ path('app_admin_produit_index') }}" class="btn btn-outline-dark">Retour aux produits</a>

			{{ include('admin_produit/_delete_form.html.twig') }}
		</div>
	</section>
{% endblock %}
