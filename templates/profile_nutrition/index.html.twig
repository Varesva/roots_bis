{% extends 'base.html.twig' %}

{% block title %}Types d'alimentation - Roots% endblock %}

{% block body %}
    <h1>Les types d'alimentation</h1>
<p>
Retrouvez les restaurants et livres de recettes disponibles sur Roots qui proposent des plats et recettes en accord avec votre régime alimentaire !
</p>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Regime</th>
                <th>Image</th>
                <th>Definition</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for nutrition in nutrition %}
            <tr>
                <td>{{ nutrition.id }}</td>
                <td>{{ nutrition.regime }}</td>
                <td>{{ nutrition.image }}</td>
                <td>{{ nutrition.definition }}</td>
                <td>
                    <a href="{{ path('app_profile_nutrition_show', {'id': nutrition.id}) }}">show</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
