{% extends 'base.html.twig' %}

{% block title %}Admin - Afficher une Commande - Roots
{% endblock %}


{% block body %}
	<h1>Afficher une Commande</h1>
	<section class="container">
		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ commande.id }}</td>
				</tr>
				<tr>
					<th>Date</th>
					<td>{{ commande.date ? commande.date|date('Y-m-d H:i:s') : '' }}</td>
				</tr>
			</tbody>
		</table>
		<div class="d-flex justify-content-around">
			<a href="{{ path('app_admin_commande_edit', {'id': commande.id}) }}" class="btn btn-outline-warning">Modifier</a>

			<a href="{{ path('app_admin_commande_index') }}" class="btn btn-outline-dark">Retour à la liste des commandes</a>

			{{ include('admin_commande/_delete_form.html.twig') }}
		</div>
	</section>

{% endblock %}
