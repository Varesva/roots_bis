{% extends 'base.html.twig' %}

{% block title %}Boutique{% endblock %}

{% block body %}
    <h1>Boutique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ boutique.id }}</td>
            </tr>
            <tr>
                <th>Type_produit</th>
                <td>{{ boutique.typeProduit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_boutique_index') }}">back to list</a>

    <a href="{{ path('app_admin_boutique_edit', {'id': boutique.id}) }}">edit</a>

    {{ include('admin_boutique/_delete_form.html.twig') }}
{% endblock %}
