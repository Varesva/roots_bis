{% extends 'base.html.twig' %}

{% block title %}Modifier un Restaurant
{% endblock %}

{% block body %}
	<h1>Modifier Restaurant</h1>

	<section class="container">

		{{ include('admin_restaurant/_form.html.twig', {'button_label': 'Modifier'}) }}
		<hr>

		<div class="d-flex justify-content-around flex-wrap">

			<a href="{{ path('app_admin_restaurant_index') }}" class="btn btn-outline-dark">Retour à la liste des restaurants</a>

			{{ include('admin_restaurant/_delete_form.html.twig') }}

		</div>

	</section>

{% endblock %}
