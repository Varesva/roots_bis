{% extends 'base.html.twig' %}

{% block title %}Admin - Modifier un Restaurant - Roots
{% endblock %}

{% block body %}
	<h1>Modifier ce Restaurant</h1>
	<section class="container">

		{{ include('admin_restaurant/_form.html.twig', {'button_label': 'Mettre à jour et modifier'}) }}
		<hr>

		<div class="d-flex justify-content-around">
			<a href="{{ path('app_admin_restaurant_index') }}" class="btn btn-outline-dark">Retour à la liste des restaurants</a>

			{{ include('admin_restaurant/_delete_form.html.twig') }}
		</div>
	</section>

{% endblock %}
