{% extends 'base.html.twig' %}

{% block title %}Afficher Restauration
{% endblock %}

{% block body %}
	<h1>Afficher Restauration</h1>

	<section class="container">
		<div class="table-responsive-lg">

			<table class="table table-striped table-bordered">
				<tbody>
					<tr>
						<th>Id</th>
						<td>{{ restauration.id }}</td>
					</tr>
					<tr>
						<th>Type_cuisine</th>
						<td>{{ restauration.typeCuisine }}</td>
					</tr>
				</tbody>
			</table>
		</div>
		<div class="d-flex justify-content-center flex-wrap">
			<a href="{{ path('app_admin_restauration_index') }}" class="btn btn-outline-dark">Retour à la liste des catégories</a>


			<a href="{{ path('app_admin_restauration_edit', {'id': restauration.id}) }}" class="btn btn-outline-warning">Modifier</a>


			{{ include('admin_restauration/_delete_form.html.twig') }}
		</div>
	</section>
{% endblock %}
