{% extends 'base.html.twig' %}

{% block title %}Nutrition index{% endblock %}

{% block body %}
    <h1>Nutrition index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Regime</th>
                <th>Image</th>
                <th>Definition</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for nutrition in nutrition %}
            <tr>
                <td>{{ nutrition.id }}</td>
                <td>{{ nutrition.regime }}</td>
                <td>{{ nutrition.image }}</td>
                <td>{{ nutrition.definition }}</td>
                <td>
                    <a href="{{ path('app_admin_nutrition_show', {'id': nutrition.id}) }}">show</a>
                    <a href="{{ path('app_admin_nutrition_edit', {'id': nutrition.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_nutrition_new') }}">Create new</a>
{% endblock %}
