{% extends 'base.html.twig' %}

{% block title %}Nutrition{% endblock %}

{% block body %}
    <h1>Nutrition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nutrition.id }}</td>
            </tr>
            <tr>
                <th>Regime</th>
                <td>{{ nutrition.regime }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ nutrition.image }}</td>
            </tr>
            <tr>
                <th>Definition</th>
                <td>{{ nutrition.definition }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_nutrition_index') }}">back to list</a>

    <a href="{{ path('app_admin_nutrition_edit', {'id': nutrition.id}) }}">edit</a>

    {{ include('admin_nutrition/_delete_form.html.twig') }}
{% endblock %}
