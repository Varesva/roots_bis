{% extends 'base.html.twig' %}

{% block title %}
	Modifier User
{% endblock %}

{% block body %}
	<div class="bg_admin">
		<h1>Modifier User</h1>

		<div class="container">

			{{ include('admin_user/_form.html.twig', {'button_label': 'Modifier'}) }}
			<hr>
			<div class="d-flex justify-content-center flex-wrap">
				<a href="{{ path('app_admin_user_index') }}" class="btn btn-light ">Retour User index</a>

				<a href="{{ path('app_admin_user_show', {'id': user.id}) }}" class="btn btn-primary ">Afficher User</a>

				{{ include('admin_user/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}
